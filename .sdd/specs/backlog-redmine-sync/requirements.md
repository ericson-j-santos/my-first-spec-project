# Requirements Document

## Project Description (Input)
Especificar o fluxo completo de publicação de um requisito do ReqSys no Redmine via endpoint POST /v1/backlog/publicar-redmine/{id}. Inclui integração com github_redmine.py (busca issues GitHub) e wiki_publisher.py (gera conteúdo Markdown para wiki Redmine). Projeto: reqsys-v2-enterprise-real, backend/app/api/pipeline.py.

## Requirements
<!-- Will be generated in /sdd:spec-requirements phase -->
